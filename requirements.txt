# Global requirements for the archived experiment scripts.
#
# These match the geometric-v1 loss3 GPU environment used for the runs. PyTorch
# is intentionally installed by linux-gpu/install_a6000.sh before this file so
# the correct CUDA wheel can be selected for the target machine.
numpy>=1.22,<=1.24.3
pillow>=10.0
scipy>=1.10
opencv-python>=4.8,<4.9
opencv-python-headless>=4.8,<4.9
diffusers @ git+https://github.com/huggingface/diffusers.git
transformers>=4.41
accelerate>=0.30
safetensors>=0.4
requests>=2.32
deepface>=0.0.93
tensorflow==2.12.1
keras==2.12.0
albumentations==1.3.1
